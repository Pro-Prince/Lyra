/**
 * Hero portrait helper asset.
 * The hero portrait is generated live from the 3D VRM model (lyra.vrm) via outfitCache / generateHeroPortrait.
 * No flat SVG vector placeholder illustration is used.
 */

export const DEFAULT_HERO_PORTRAIT = "";

